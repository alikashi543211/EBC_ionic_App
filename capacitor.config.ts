import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'elbow-beach-cycle',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
